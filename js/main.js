const menuBtn = document.querySelector('.menu-btn');
const hamburger = document.querySelector('.menu-btn__burger');


menuBtn.addEventListener('click', () => {
    menu-btn__burger.classList.toggle('.change');
});
